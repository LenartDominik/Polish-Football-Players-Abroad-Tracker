# Streamlit Cloud Requirements
# Frontend requirements - komunikacja z FastAPI backend

# Core Streamlit
streamlit==1.51.0

# Data processing and visualization
pandas==2.3.3
plotly==5.18.0
numpy==2.3.5

# API communication (REQUIRED - frontend uses API now)
requests==2.32.5

# NO DATABASE DEPENDENCIES NEEDED
# Frontend now communicates with backend API instead of direct database access
# psycopg2-binary - NOT NEEDED (backend handles database)
# sqlalchemy - NOT NEEDED (backend handles database)
# sqlite3 - NOT NEEDED (backend handles database)

# Environment configuration
python-dotenv==1.2.1

# Note: Make sure to set API_BASE_URL environment variable:
# - Local: http://localhost:8000 (default)
# - Production: https://your-backend.onrender.com
